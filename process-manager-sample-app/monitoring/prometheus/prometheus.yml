global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: process-manager-sample-app
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - app:8080
